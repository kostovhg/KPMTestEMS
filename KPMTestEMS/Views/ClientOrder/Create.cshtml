@model KPMTestEMS.Models.ProductionViewModel

@{
    ViewBag.Title = "Order";
}
<script type="text/javascript" src="~/Scripts/jquery-1.12.4.min.js"></script>
<script type="text/javascript" src="~/Scripts/jquery-ui-1.12.1.min.js"></script>
@*<script type="text/javascript" src="~/Scripts/DatePickerReady.js"></script>*@



<script type="text/javascript">
    $(function () {
        $("#datepicker").datepicker(
            {
                changeMonth: true,
                changeYear: true,
                dateFormat: 'dd-mm-yy',
                showOn: "both"
            }
        );

    });
</script>
<style type="text/css">
    .ui-datepicker {
        font-size: 8pt !important;
    }
</style>


<div class="container col-lg-10 col-md-10 col-sm-12 col-xs-12">
    <div class="page-header">
        <h2>Create order</h2>
    </div>
    <p>Choose properties and width</p>

    <div class="well">
        @using (Html.BeginForm("CreateOrder", "ClientOrder", FormMethod.Post, new { @class = "form-horizontal"}))

        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary()

            <div class="form-group">
                <label class="control-label col-sm-4" for="select_brand">Select Brand:</label>
                <div class="col-md-4">
                    @foreach (var item in Model.BrandList)
                    {
                        <div>@Html.RadioButtonFor(s => s.SelectedBrand, item.Value, new { @class = "radio-inline" , name="select_brand"})@item.Text</div>
                    }
                </div>

            </div>
            @*<div class="form-group">
                <label for="select_weight" class="control-label col-md-4">Select weight</label>
                <div class="col-md-4">
                    @Html.DropDownList("select_weight", new { Model.WeightList}, null)
                </div>
            </div>*@
        @*<div class="form-group">
                <label for="select_weight" class="control-label col-md-4">Select weight</label>
                <div class="col-md-4">
                    <select class="form-control col-sm-4" id="select_weight">
                        @foreach (var weight in Model.WeightList)
                        {
                            @Html.Op
                            <option>@weight.WeightValue</option>
                        }
                    </select>
                </div>
            </div>*@

                    @*<div class="form-group">
                        <label for="select_weight" class="control-label col-sm-4">Select weight</label>
                        <div class="col-sm-4">
                            <select class="form-control col-xs-4 col-sm-4" id="select_weight">

                            </select>
                        </div>*@
                    @*<div class="col-sm-push-3">
                            <p>Grams per square meter</p>
                        </div>*@
                    @*</div>*@

                    @*<div class="form-group">
                        <label for="select_width" class="control-label col-sm-4">Select width</label>
                        <div class="col-sm-4">
                            <select multiple="" class="form-control col-xs-4 col-sm-4" id="select_width">*@
                    @*@foreach (var width in Model.Width)
                                    {
                                        <option>@width.PaperWidth</option>
                                    }
                                </select>
                            </div>
                        </div>*@

    <div class="form-group">
        @Html.LabelFor(m => m.DueDate, new { @class = "control-label col-sm-4", @for = "datepicker" })
        <div class="col-sm-4">
            @Html.TextBoxFor(m => m.DueDate, "{0:dd-MM-yyyy}", new { @class = "form-control col-xs-4 col-sm-4 datepicker", @id = "datepicker" })
        </div>

    </div>

                    @*<div class="form-group">
                            <label for="textArea" class="control-label col-sm-4">Your comments</label>
                            <div class="col-sm-4">
                                <textarea class="form-control col-xs-4 col-sm-4" rows="3" id="textArea"></textarea>
                            </div>
                        </div>*@

    <div class="form-group">
        <div class="col-sm-4 col-sm-offset-4">
            <button type="reset" class="btn btn-default">Cancel</button>
                            @*@Html.ActionLink("Order", "CreateOrder", "ClientOrder", null, new { @class = "btn btn-primary", type = "submit" })*@
            <input type="submit" value="Send" class="btn btn-primary" />
        </div>
    </div>
        }

</div>
</div>
